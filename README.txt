Insight uses Unity and Mirror to provide a seperate network connection outside of NetworkManager. This allows connection to a MasterServer.

Built on Unity 2017.4 but should work with any newer version of Unity.